//exists so the program can be run on the command line (can't directly run JavaFX applications from there)
public class Main {

    public static void main(String[] args) {
        UserInterface.main(args);
    }
}
